USING: tools.deploy.config ;
V{
    { strip-word-props? t }
    { strip-word-names? t }
    { strip-dictionary? t }
    { strip-debugger? t }
    { deploy-math? t }
    { deploy-compiled? t }
    { deploy-io? t }
    { deploy-ui? t }
    { "bundle-name" "Bunny.app" }
}
