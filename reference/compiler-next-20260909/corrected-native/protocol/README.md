The c694 preparation explicitly reloaded classes.algebra, SSA phases, then backtracking and rebuilt the selected closure. Its new all-memory synthetic unit fixture failed under native rematerialization because it used constant inputs. The a7 update changes only tests/docs, preserving production bytes, and reparses the fixed fixture from runtime peeks. Both failed and successful evidence are retained. The original c694 prepared image was retained separately before a7 preparation.

The capture is an untimed generate hook around actual closure compilation and installation. It records the requested WORK words; it is not loaded into any timed process or saved prepared image. It does not capture all transitive callees, nor retroactively identify code from the earlier timed processes. The successful original-baseline capture is reused only after checking its status, source, record count, and exact script hashes.
