# Backtracking interior-gap runtime attribution

Index-only `dda193f12ad49aedaa91a1d1c522c4622ed63de3` versus the narrow gap policy `7e3f572c19`, native Linux x86-64 on agent1, CPU 2. Both use the identical VM and prepared index image; fresh processes explicitly reload the exact recorded backtracking source. This is a targeted diagnostic pair, separate from the final fully refreshed combined-source matrix.

The public backtracking allocator compiles the frozen `ffi-pressure` kernel and workload. Calls execute namespace-held word objects dynamically, with 32 independent numerical oracle checks per process. SSA, interval and final value-flow verification, including rematerialization verification, are enabled; rematerialization and loop spilling are on and GVN is off. Each measured batch executes 600 workload iterations (19.2 million kernel calls). One warm-up per process is excluded. Two reversed pairs provide six measured batches per revision.

| Median | Index | Gap policy | Candidate/baseline |
|---|---:|---:|---:|
| Runtime CPU seconds | 0.3645521595 | 0.3436884895 | 0.942769 |
| Retired instructions | 6,327,605,259.5 | 6,135,611,689 | 0.969658 |
| Generated kernel bytes | 1,984 | 2,000 | 1.008065 |

Both run orders improve CPU time and retired instructions, despite the 16-byte code increase. The accepted policy skips only strictly interior, single-block gaps with fewer than two adjacent mandatory fragments of the same original value. It preserves CFG-boundary and multi-block bridges. Exact commands, backend hashes, strict-check outcomes, load snapshots and all warm-up/measured observations are retained in the raw logs and statuses. `summary.json` is computed from the six non-warm-up samples. Static IR copy counts are not emitted move counts; this result uses actual retired instructions and generated bytes.
