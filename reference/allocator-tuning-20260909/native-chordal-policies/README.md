# Chordal entry-state policy runtime attribution

Baseline `7b6cd9519a71960a4fb0385bf6ceeaf66097d618` versus combined chordal policies `bd2edfd3a17440b25bc8fd0da944f41ea04d885b`, native Linux x86-64 on agent1, CPU 2. Candidate spilling source SHA-256 is `0b99de1ca0b34495605f7750c24bec261b7b51d43c13b87c1bdb83c97509f7df`. The same VM and baseline prepared image are used; each fresh diagnostic process explicitly reloads spilling and chordal code. Final combined-source matrices separately use fresh full preparation.

The first policy excludes an entry register requirement only when every predecessor plan is known and no predecessor holds that value in a register. The second intersects known predecessor memory states for resident entry values. Unknown loop predecessors retain conservative handling. First-policy source traces are preserved in `../native-chordal-entry-filter`; the second-policy trace here records final block plans and rewritten instructions. Both execute both branch paths with answers 3,672 and 16,712.

The public chordal allocator compiles the unchanged `branch-pressure` kernel and workload. Namespace-held word objects are called dynamically, with 32 independent scalar oracle checks per process. All allocation checks and the final rematerialization-aware verifier are enabled; rematerialization and loop spilling are on, GVN off. One warm-up per process is excluded. Two reversed pairs provide six batches per revision, each containing 600 workload iterations (19.2 million kernel calls).

| Median | Baseline | Candidate | Candidate/baseline |
|---|---:|---:|---:|
| Runtime CPU seconds | 0.3048745230 | 0.2372253295 | 0.778108 |
| Retired instructions | 5,665,754,245.5 | 5,377,633,897 | 0.949147 |
| Generated kernel bytes | 2,256 | 2,048 | 0.907801 |

Both reversed pairs improve. Exact scripts, backend hashes, commands, load snapshots, checked outputs and warm-up/measured observations are retained. These targeted results do not rank allocators over the broader corpus. Source-trace spills/reloads/copies are machine-IR operations; generated bytes and retired instructions are actual machine-code measurements.
