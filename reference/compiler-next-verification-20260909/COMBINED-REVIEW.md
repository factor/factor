# Combined production review

Reviewed the production diff from `c1f7e4d34cd604bbdf22576b237406b39758896d` to `eccfc0accc5c513b44701f2e59d4a9bafb7515f0` in `compiler-next-integration` on 2026-09-09. This was a read-only source review: no new tests, builds, performance jobs, or integration edits were performed. No correctness concern was found within the supported input domains.

- **Class algebra:** the shortcut requires identical operands that are words. Valid named class operands already return that same word through the old comparison/cache path. Anonymous classoids retain the original structural-cache behavior, including cached aliases and opaque-predicate identity effects. This reasoning assumes valid class operands; it does not promise unchanged error behavior for nonclass words passed to the algebra API.
- **Greedy hints:** the scalar query retains split-hint contribution, duplicate copy-hint weights, both interval coverage checks, and current peer register assignments. It introduces no assignment cache. The empty-score path preserves bank order, matching the old stable sort of equal scores.
- **Backtracking entry transports:** the rewrite clears only spill-slot reloads at exact block entries for original live-in values. It excludes phi, GC, and clobber entry instructions, kill successors, and any kill predecessor; predecessors must be nonempty. Existing spill obligations remain intact. Phase assignment consequently publishes the allocated register as the incoming destination, and existing parallel SSA edge resolution supplies each predecessor's actual register or memory value. Transport validation still precedes the rewrite. Correctness relies on the existing SSA liveness, interval, and edge-map contracts; no memory-cleanliness assumption is added.
- **Chordal selection:** the caller constructs free colors by filtering ascending `<iota>` and rejects exhaustion before selection. A scan updating only on a strictly larger score therefore retains the smallest free color on ties, exactly matching the former lexicographic `[-score, color]` sort. Production affinity weights are finite exact numbers; arbitrary unordered scores are outside this reasoning.

The earlier independent coloring corpus and its recorded execution remain separate evidence in this directory. This combined review makes no new execution or performance claim.
